# 役割分担

- **Claude Code**: 設計・レビューのみ行う
- **Codex**: コードの実装を担当する

## コード実装のルール

コードを書く作業は必ずMCP経由でCodexに投げること。Claude Code自身がコードを書かない。

Codexにコードを依頼するときは、実装内容だけでなく**設計思想**（なぜそう設計するか、どういう方針で実装するか）も必ず一緒に伝えること。
