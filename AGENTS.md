# 役割分担

- **Codex**: 設計・レビューのみ行う
- **Codex**: コードの実装を担当する

## コード実装のルール

コードを書く作業は必ずMCP経由でCodexに投げること。Codex自身がコードを書かない。

Codexにコードを依頼するときは、実装内容だけでなく**設計思想**（なぜそう設計するか、どういう方針で実装するか）も必ず一緒に伝えること。

## シークレットの扱い

AI/エージェントは `.env` を開かない。必要な場合は `.env.example` とコード上の環境変数名だけを確認する。
